# 图书管理系统 技术报告

> 项目路径:`C:\Users\28403\Desktop\C_Project`
> 报告日期:2026-08-12
> 代码规模:5 个源文件,约 440 行 C 代码(C99)

---

## 1. 项目概述

### 1.1 项目定位

本项目是一个基于 **C 语言** 实现的 **图书管理系统** 命令行程序,以「单链表 + 线性查找」为核心技术方案,实现图书数据的组织、遍历与多条件查询。程序通过控制台菜单与用户交互,内置 8 条样例数据,支持查询、浏览、退出三类主功能。

### 1.2 功能清单

| 主菜单项 | 功能说明 | 实现入口 |
|---|---|---|
| 1. 用户功能-图书查询 | 进入子菜单,支持 5 种条件查询 | `queryMenu()` |
| 2. 浏览全部图书 | 遍历链表逐本打印全部图书 | `showAll()` |
| 0. 退出 | 释放链表内存后退出程序 | `main()` 中 `freeList()` |

查询子菜单(5 种条件):

| 子菜单项 | 查询条件 | 匹配方式 |
|---|---|---|
| 1 | 书名 | 包含式模糊(`strstr`) |
| 2 | 作者 | 包含式模糊(`strstr`) |
| 3 | 出版社 | 包含式模糊(`strstr`) |
| 4 | 出版年份 | 精确相等 |
| 5 | 库存数量 | 比较运算(≤ / ≥ / =) |

### 1.3 技术要点

- **语言标准**:C99(使用 `for(int i = 0; ...)` 循环声明、结构体整体按值赋值);
- **数据结构**:自定义单向链表 `BookNode`,头指针类型 `BookList`;
- **查询算法**:全表线性扫描,O(n),无任何索引结构;
- **内存管理**:手动 `malloc` / `realloc` / `free`,无第三方库依赖;
- **模块划分**:数据层(`book`)、查询层(`query`)、交互层(`main`)三层分离。

---

## 2. 文件结构与模块划分

### 2.1 文件清单

| 文件 | 类型 | 职责 | 实际行数 |
|---|---|---|---|
| `book.h` | 头文件 | `Book` 结构体、链表节点、链表操作函数声明 | 36 |
| `book.c` | 实现文件 | 链表创建 / 尾插 / 求长 / 遍历 / 释放 | 57 |
| `query.h` | 头文件 | `QueryResult` 结构体、查询函数声明 | 25 |
| `query.c` | 实现文件 | 5 种条件查询 + 结果集扩容管理 | 95 |
| `main.c` | 入口文件 | 菜单交互、输入处理、格式化输出、样例数据 | 228 |

合计 441 行。

### 2.2 模块依赖关系

```
          main.c
         /      \
   book.h        query.h
     |              |
   book.c        query.c ──→ book.h
```

依赖要点:

- `book.h` 是数据层基础,定义 `Book` 与链表结构;
- `query.h` 通过 `#include "book.h"` 依赖数据层;
- `main.c` 同时包含两个头文件,是唯一与用户交互的模块;
- 所有头文件均带 include guard(`#ifndef` / `#define` / `#endif`),可被安全重复包含;
- 所有 `.c` 文件均以引号形式包含项目头文件(如 `#include "book.h"`),编译时无需额外 `-I.` 参数,可移植性好。

---

## 3. 核心数据结构

### 3.1 `Book` —— 图书实体

```c
typedef struct {
    char id[20];        /* 图书编号 */
    char name[50];      /* 书名 */
    char author[30];    /* 作者 */
    char publisher[50]; /* 出版社 */
    int year;           /* 出版年份 */
    double price;       /* 价格(元) */
    int total;          /* 馆藏总数 */
    int stock;          /* 库存数量 */
} Book;
```

字段设计说明:

| 字段 | 类型 | 检索用途 |
|---|---|---|
| `id` | `char[20]` | 唯一编号(样例:B001 ~ B008) |
| `name` | `char[50]` | 书名模糊匹配 |
| `author` | `char[30]` | 作者模糊匹配 |
| `publisher` | `char[50]` | 出版社模糊匹配 |
| `year` | `int` | 年份精确匹配 |
| `price` | `double` | 价格(仅展示) |
| `total` | `int` | 馆藏总数(仅展示) |
| `stock` | `int` | 库存比较查询 |

字符串字段采用**定长数组**而非指针:优点是结构体可以整体按值拷贝(`newNode->data = b` 一条语句完成赋值),内存紧凑、无额外分配;缺点是长度受限(书名超 49 字符会截断)。

### 3.2 `BookNode` —— 链表节点

```c
typedef struct BookNode {
    Book data;              /* 数据域:一本图书 */
    struct BookNode *next;  /* 指针域:指向后继节点 */
} BookNode;
```

经典单链表节点,`data` 直接内嵌 `Book` 值(非指针),节点即完整图书记录。

### 3.3 `BookList` —— 头指针类型

```c
typedef BookNode *BookList;
```

链表只保留**头指针**,不维护尾指针 `tail`、不缓存节点计数 `size`。这一取舍直接决定了尾插与求长均为 O(n)(详见第 4 节)。

### 3.4 `QueryResult` —— 查询结果集

```c
typedef struct {
    BookNode **nodes;   /* 命中节点指针数组 */
    int count;          /* 命中数量 */
    int ok;             /* 1=结果完整, 0=内存不足导致结果不完整 */
} QueryResult;
```

设计亮点:

- 命中后**不复制 Book 本体**,只保存指向链表节点的指针,零数据拷贝;
- `nodes` 由 `realloc` 按需动态扩容,长度不设上限;
- `ok` 标志位记录分配失败:扩容失败时置 0,上层 `showResult` 据此打印"内存不足"警告,避免"结果不完整"被静默吞掉;
- **所有权边界清晰**:指针数组归查询层所有(由 `freeQueryResult` 释放),节点本身仍归链表层所有(由 `freeList` 释放),两层互不越界。

---

## 4. 核心算法与复杂度分析

### 4.1 函数复杂度总览

| 函数 | 时间复杂度 | 空间复杂度 | 说明 |
|---|---|---|---|
| `createList` | O(1) | O(1) | 直接返回 NULL 空链表 |
| `appendBook` | O(n) | O(1) | 尾插需遍历到表尾;批量插入 n 本总代价 O(n²) |
| `listLength` | O(n) | O(1) | 单次遍历计数 |
| `traverseList` | O(n) | O(1) | 遍历并对每个节点调用 `visit` 回调 |
| `queryByTitle/Author/Publisher/Year/Stock` | O(n) | O(k)\* | 全表线性扫描 |
| `addHit` | O(k) | O(1) | 每次命中 realloc 扩容 +1,需拷贝旧数组(见 6.3 改进第 3 条) |

> \* k 为命中数量,结果指针数组大小;节点本体不复制,故不含 O(n) 的数据拷贝。

### 4.2 查询算法统一骨架

5 种条件查询共用同一遍历框架,仅命中条件不同:

```c
QueryResult r = {NULL, 0, 1};
BookNode *p = head;
while (p != NULL) {
    if (命中条件(p->data)) {
        if (!addHit(&r, p)) break;  /* 内存不足,停止扫描 */
    }
    p = p->next;
}
return r;
```

各查询的命中条件:

| 查询函数 | 命中条件 | 匹配语义 |
|---|---|---|
| `queryByTitle` | `strstr(name, keyword) != NULL` | 包含式模糊,区分大小写 |
| `queryByAuthor` | `strstr(author, keyword) != NULL` | 包含式模糊,区分大小写 |
| `queryByPublisher` | `strstr(publisher, keyword) != NULL` | 包含式模糊,区分大小写 |
| `queryByYear` | `year == 目标年份` | 精确相等 |
| `queryByStock` | `op` 决定 `stock <=` / `>=` / `== value` | 三种比较运算 |

### 4.3 复杂度结论

- 单次查询 O(n),结果集空间 O(k),整体"结构简单、无索引、线性扫描";
- 适合数据量小(百级以内)的场景;当馆藏量增大时,线性扫描成为性能瓶颈;
- 链表本身无随机访问能力,且未缓存长度,`listLength` 每次都要 O(n) 遍历——若频繁调用可增补 `size` 字段(见 6.3)。

---

## 5. 关键函数逐模块分析

### 5.1 链表模块(`book.c`)

**`appendBook` —— 尾插(核心函数)**

```c
int appendBook(BookList *head, Book b) {
    BookNode *newNode = (BookNode *)malloc(sizeof(BookNode));
    if (newNode == NULL) return 0;              /* 内存分配失败 */
    newNode->data = b;  newNode->next = NULL;

    if (*head == NULL) {
        *head = newNode;                        /* 空链表:新节点即头 */
    } else {
        BookNode *p = *head;
        while (p->next != NULL) p = p->next;    /* 走到表尾 */
        p->next = newNode;
    }
    return 1;
}
```

要点:

- 使用二级指针 `BookList *head`,使得空链表时也能直接改写头指针;
- 返回 1/0 表示成功/失败——`main.c` 的 `initSampleData` 已检查返回值并在失败时提示"内存不足,样例数据加载不完整!",不再静默丢失数据;
- 每次插入都 malloc 一个新节点,批量插入 n 本代价 O(n²)。

**`freeList` —— 释放整条链表**

```c
BookNode *temp = p->next;   /* 先保存后继 */
free(p);                    /* 再释放当前 */
p = temp;
```

先存后继再释放,避免"释放后访问野指针",写法正确,无内存泄漏。

**`traverseList` —— 遍历回调**

```c
void traverseList(BookList head, void (*visit)(Book *b))
```

以函数指针把"遍历"与"处理"解耦,`main.c` 中的 `printBook` 作为回调传入,复用于"浏览全部"与"查询结果展示",设计良好。

### 5.2 查询模块(`query.c`)

**`addHit` —— 结果集动态扩容**

```c
static int addHit(QueryResult *r, BookNode *node) {
    BookNode **newNodes = (BookNode **)realloc(r->nodes, (size_t)(r->count + 1) * (sizeof(BookNode *)));
    if (newNodes == NULL) {
        r->ok = 0;  /* 标记结果不完整 */
        return 0;
    }
    r->nodes = newNodes;
    r->nodes[r->count] = node;
    r->count++;
    return 1;
}
```

要点:

- 返回 0 表示扩容失败,由调用方 `break` 停止扫描;失败信息通过 `r->ok` 标志向上层传递,`showResult` 会打印"警告:内存不足,查询结果可能不完整!"——错误不再被静默吞掉;
- 每次命中都 `realloc` 一次,命中数量多时存在频繁扩容拷贝开销(改进见 6.3 第 3 条);
- 库函数内不再直接 `printf`,纯数据化,便于复用与测试。

**`queryByStock` —— 库存比较查询**

以 `op` 参数编码三种比较语义(`1` 表示 `<=`、`2` 表示 `>=`、`3` 表示 `==`),接口紧凑;`main.c` 查询子菜单已在调用前校验 `op < 1 || op > 3` 为非法输入并提示重新输入,非法值不会再静默返回空结果。

**`freeQueryResult`** 只 `free(r->nodes)` 并复位(`nodes=NULL, count=0, ok=1`),不触碰链表节点——所有权边界正确。

### 5.3 交互入口(`main.c`)

**输入处理**:`readLine` 用 `fgets` 读取并去掉末尾换行,`clearInput` 清空 scanf 残留的缓冲字符,正确处理了"scanf 读整数 + fgets 读字符串"混用时的换行问题,这是本项目输入层做得比较细致的地方。

**scanf 返回值检查**:主菜单与查询子菜单均检查 `scanf` 返回值——`EOF` 时(如管道关闭)直接退出,`!= 1` 时提示"输入无效"并清缓冲重来,避免非数字输入导致变量未初始化。

**样例数据**:`initSampleData` 用 `Book samples[] = { ... }` 数组初始化一次构造 8 条样例数据,覆盖不同年份、出版社、库存(含 0 库存)等典型情况;逐条 `appendBook` 尾插建表。

**菜单循环**:主菜单与查询子菜单均为 `while(1) + switch` 结构,`0` 退出;主菜单 `0` 分支与输入流结束(EOF)两条路径在退出前都会调用 `freeList(list)` 释放整条链表,基本做到"有借有还"。

**中文对齐**:`displayWidth` 按 UTF-8 字节模式计算显示宽度(三字节 CJK 记 2 列),`printPadded` 按该宽度补齐空格,`printBook` 据此对齐打印,解决了中文多字节字符导致的列错位问题。

---

## 6. 已知问题与改进建议

### 6.1 历史缺陷与修复记录

早期版本存在以下缺陷,已在当前代码中全部修复,此处留存记录以便对比:

| # | 历史缺陷 | 修复前后果 | 当前修复情况 |
|---|---|---|---|
| 1 | 查询子菜单 case 2/3/4 误调 `queryByTitle`(复制粘贴错误) | 按作者、出版社、年份查询时实际按书名匹配,结果错误 | 已修复:case 2 → `queryByAuthor`、case 3 → `queryByPublisher`、case 4 → `queryByYear` |
| 2 | 年份查询把年份当字符串读取(`readLine` + 当书名关键字) | 年份查询功能失效 | 已修复:`scanf("%d", &year)` 读整数 + `queryByYear(head, year)` |
| 3 | 库存比较 `op` 无校验,非法值静默返回空结果 | 用户无提示,查询结果误导 | 已修复:菜单层 `if (scanf("%d", &op) != 1 \|\| op < 1 \|\| op > 3)` 拦截并提示重新输入 |

修复后,5 种条件查询在 `main.c` 中的调用均为:

```c
case 1:  r = queryByTitle(head, kw);      /* 书名 */
case 2:  r = queryByAuthor(head, kw);     /* 作者 */
case 3:  r = queryByPublisher(head, kw);  /* 出版社 */
case 4:  r = queryByYear(head, year);     /* 年份 */
case 5:  r = queryByStock(head, op, value);  /* 库存 */
```

### 6.2 健壮性复核

早期报告列出的健壮性问题,经逐条复核:

| # | 历史问题 | 复核结论 |
|---|---|---|
| 4 | `appendBook` 返回值未检查,内存分配失败时数据静默丢失 | **已解决**:`initSampleData` 检查返回值并提示"内存不足,样例数据加载不完整!" |
| 5 | `addHit` realloc 失败仅 printf,结果不完整用户无感知 | **已解决**:改为 `ok` 标志位 + 返回错误码,`showResult` 打印警告 |
| 6 | `scanf` 返回值未检查,非数字输入导致变量未初始化 | **已解决**:主菜单、子菜单、年份、库存输入均检查 `scanf` 返回值,`EOF` 直接退出 |
| 7 | `printBook` 用 `%-10s` 对齐导致中文列错位 | **已解决**:改用 `displayWidth` + `printPadded` 按显示宽度补齐 |
| 8 | `book.c` 用尖括号 `#include <book.h>`,依赖 `-I.` 编译参数 | **已解决**:统一为引号 `#include "book.h"`,无需额外参数 |

### 6.3 架构与性能改进方向

以上问题已闭环,后续可选的增强方向(按性价比排序):

1. **索引加速**:为书名/作者建哈希索引,或维护按年份排序的数组 + 二分查找,把单次查询从 O(n) 降到 O(log n) 乃至 O(1);
2. **尾指针 + 计数优化**:`BookList` 增补 `tail` 与 `size` 字段,尾插降为 O(1),批量建表整体降为 O(n);
3. **结果集批量扩容**:`addHit` 改为容量倍增(如 ×2)策略,减少 realloc 拷贝次数,扩容均摊代价从 O(k) 降为 O(1);
4. **文件持久化**:增加 `saveBooks` / `loadBooks`(文本或二进制),退出保存、启动加载,避免每次运行重建数据;
5. **库存查询库内兜底校验**:`queryByStock` 内部对 `op` 再校验一次(如非法值直接返回空结果并置 `ok=0`),使查询库在脱离菜单层使用时也保持健壮;
6. **UI 与逻辑分离**:将输出逻辑集中在 `main.c`,保持 `query.c` 纯数据化(当前已基本做到,可继续推行)。

---

## 7. 运行说明与总结

### 7.1 编译运行

头文件均以引号形式包含,直接编译即可,无需 `-I.` 参数:

```bash
gcc main.c book.c query.c -o book_manager
./book_manager          # Windows 下运行 book_manager.exe
```

> 已用 `gcc -std=c99 -Wall -Wextra -fsyntax-only main.c book.c query.c` 验证,无警告、无错误。

### 7.2 使用流程

1. 启动后进入主菜单:选 **1** 查询、**2** 浏览全部图书、**0** 退出;
2. 选择「1.用户功能-图书查询」进入子菜单,可按书名 / 作者 / 出版社 / 年份 / 库存 5 种条件查询,均已按正确条件匹配;
3. 选择「2.浏览全部图书」,打印全部 8 本样例数据(当前共有 8 本);
4. 选择「0.退出」,释放链表内存并结束程序。

### 7.3 总结

本项目是一个结构清晰、注释完整的小型 C 语言课程设计:

- **优点**:数据层(`book`)与查询层(`query`)分层合理;链表内存管理正确(创建、插入、释放闭环,无泄漏);遍历回调、结果集零拷贝指针存储、`ok` 标志位错误传递等设计简洁优雅;输入层对 scanf / fgets 混用缓冲、`scanf` 返回值检查、EOF 退出的处理细致;中文对齐问题已用显示宽度补齐解决;
- **现状**:早期版本查询子菜单的复制粘贴错误与年份输入逻辑错误均已修复,5 种查询可正确工作;早期健壮性问题(返回值未检查、`op` 未校验、尖括号包含等)已逐条闭环。

当前代码可作为「单链表 + 线性查找」的教学范例稳定运行;若进一步实施 6.3 节的优化(索引、尾指针、批量扩容、文件持久化),可平滑扩展到更大规模的馆藏数据。

---

*报告完 — 基于 `book.h` / `book.c` / `query.h` / `query.c` / `main.c` 五个源文件的源码逐行分析生成,与当前代码状态一致。*
